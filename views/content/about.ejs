<% include ../partials/header.ejs %>

    <!-- MAIN CONTENT -->
    <div class="container">
      <div class="row">
        <div class="col-md-offset-3 col-md-6">
          <h1><%= title %></h1>
          <p>About Me</p>
          <ul>
            <% for(var count=0; count < names.length; count++) { %>
              <li><a href="#"><%= names[count] %></a></li>
            <% } %>
          </ul>
        </div>
      </div>
    </div>

<% include ../partials/footer.ejs %>


